import { OwnerInterceptorInterceptor } from '../owner-interceptor.interceptor';

describe('OwnerInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new OwnerInterceptorInterceptor()).toBeDefined();
  });
});
